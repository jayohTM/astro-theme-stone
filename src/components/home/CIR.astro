
---
const res = await fetch("https://www.coininterestrate.com/wp-json/wp/v2/platforms");
//const res = await fetch("https://www.coininterestrate.com/coin-api-json/"); 
const posts = await res.json();
---





<section class="py-16 sm:py-20">
    <section class="py-16 sm:py-20">
        <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
          <div class="flex flex-col gap-12 sm:gap-16">
            <h2 class="text-3xl font-medium tracking-tight sm:text-4xl">Platforms</h2>

            {
                posts.map((post) => (
                    <h2 set:html={post.title.rendered} />
                    <p set:html={post.excerpt} />
                    <a href={post.slug}>Link</a>
                    // <p set:html={post.slug} />
                    // <p set:html={post.content.rendered} />
                ))
            }


            
            </div>
        </div>
    </section>
</section>
